# WIKA 报告改版条件判断 STAGE52

## 结论

改版条件：`NOT_READY`

当前没有真实反馈，也没有真实补数，因此不允许修改报告正文，不允许升级广告、页面、产品、销售或订单相关报告结论。

## 判断依据

- 真实反馈数量：`0`
- 示例反馈数量：`1`
- 真实补数 received 数：`0`
- 人工补数 waiting owner 数：`5`
- 6 个角色仍缺真实联系人。
- 6 个角色尚无真实发送记录。

## 分支规则

1. 没有真实反馈，也没有真实补数：
   - 改版条件 = `NOT_READY`
   - backlog 只包含等待项和准备项
   - 不允许修改报告正文
2. 有真实反馈但没有补数：
   - 只允许表达、格式、阅读顺序类改版
   - 不允许升级数据结论
3. 有真实补数：
   - 只允许生成对应报告的数据增强项
   - 仍不能写成已执行动作，除非完成新一轮报告生成
4. 写侧动作诉求：
   - 标记为 `OUT_OF_SCOPE_WRITE_ACTION`
5. GMV、转化率、国家结构、完整经营诊断：
   - 标记为 `REQUIRES_NEW_DATA_SOURCE`

## 当前下一步

先催收联系人、反馈和补数。收到真实输入后，再进入下一轮 triage 与报告改版。
